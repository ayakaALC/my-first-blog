# -*- mode: python -*-

block_cipher = None


a = Analysis(['FootPrints_Export(XLSX ver 3.56).py'],
             pathex=['C:\\Users\\has\\AppData\\Local\\Programs\\Python\\Python37-32\\Scripts'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          [],
          exclude_binaries=True,
          name='FootPrints_Export(XLSX ver 3.56)',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='FootPrints_Export(XLSX ver 3.56)')
